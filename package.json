{
  "name": "foodtruck-projekt",
  "version": "1.0.0",
  "description": "Foodtruck Management System",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "web": "npm run start --workspace=web-app",
    "mobile": "npm run start --workspace=mobile-app",
    "build:web": "npm run build --workspace=web-app",
    "build:mobile:android": "npm run build:android --workspace=mobile-app",
    "build:mobile:ios": "npm run build:ios --workspace=mobile-app"
  },
  "private": true
}
